<div data-controller="dashboard">
  <div class="banner" style="background-image: linear-gradient(to left, rgba(0,0,0,0),rgba(0,0,0,1) 130%), url(<%= image_path(@movies.last.picture) %>);">
    <div class="filmoftheday">
      <h2 class= hi-there> Welcome <%= current_user.username %>! </h2>
      <html lang="en">
      <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
      </head>
      <body>

      </body>
      </html> </h2>
      <h1 class="day">Discover the movie of the day</h1>
      <div>
      <p class="movietitle"><%=@movies.last.title %></p>
      <p class="overview"><%= @movies.last.overview %></p>
      <div class=spaceyear>
      <p class="director">Director: <%= @movies.last.director %></p>
      <p class="year">Year: <%= @movies.last.year %></p>
      <p class="rating">Rating: <%= @movies.last.rating %></p>
      </div>
      <%= link_to "Watch the movie", "#movie_path(movie)", class: "btn btn-primary" %>
      <%= link_to "Discover Watch Parties", watch_parties_path, class: "btn btn-primary" %>
      </div>
    </div>
  </div>

  <div class="movie-list">
      <% @movies.sort_by { |x| x.movie_date.start_date}.reverse.each do |movie| %>
      <% unless movie == @movies.last %>
      <%= link_to movie_path(movie), class: "title" do %>
        <div class="card" style="background-image: url('<%= image_path(movie.picture) %>');" data-image-url="<%= image_path(movie.picture) %>" data-dashboard-target="poster" data-action="mouseover->dashboard#showInfo mouseout->dashboard#hideInfo">
          <h2 class="d-none"><%= movie.title %></h2>
        </div>
         <% end %>
        <% end %>
      <% end %>
  </div>
</div>
